module.exports = {
    plugins: {
        'postcss-preset-env': {
            stage: 0,  // Puedes ajustar el nivel de compatibilidad de CSS que necesitas
        },
        'autoprefixer': {},
        // Agrega más plugins si es necesario
    },
};